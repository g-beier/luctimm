---
import PageLayout from "@/layouts/Base";
// import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
// import { getAllPosts, sortMDByDate } from "@/utils";

// const MAX_POSTS = 3;
// const allPosts = await getAllPosts();
// const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">Hello World!</h1>
		<p class="mb-4">
			With a strong foundation in Red Hat Enterprise Linux and IBM AIX, complemented by advanced
			networking knowledge, I bring a holistic skill set to the table. Over the past decade, I have
			expertly managed and optimized these systems, ensuring their reliability and security. My
			dedication to customer service has been honed through years of technical support, fostering
			excellent communication and troubleshooting abilities. In addition, my proficiency in
			networking, including a deep understanding of the OSI model, has empowered me to enhance
			system performance and fortify network security. I am eager to leverage this diverse skill set
			to contribute effectively to your organization.
		</p>
		<SocialList />
	</section>
	<!--
  <section aria-label="Blog post list" class="mt-16">
    <h2 class="title mb-4 text-xl">Latest Posts</h2>
    <ul class="space-y-4 sm:space-y-2">
      {
        allPostsByDate.map((p) => (
          <li class="flex flex-col gap-x-2 sm:flex-row">
            <PostPreview post={p} />
          </li>
          ))
        }
      {
        MAX_POSTS < allPosts.length && <li>
          <a href="/posts/" class="cactus-link">See all posts</a>
        </li>
      }

    </ul>
  </section>
-->
</PageLayout>
